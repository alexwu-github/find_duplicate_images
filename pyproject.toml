[project]
name = "find-duplicate-images"
version = "0.1.0"
description = ""
authors = [
    {name = "Alex",email = "itsalexwu@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
]

[tool.poetry]
packages = [{include = "find_duplicate_images", from = "src"}]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
